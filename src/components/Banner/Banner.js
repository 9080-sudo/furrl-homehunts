import React from 'react'

import classes from './Banner.module.css'

export default function Banner() {
  return (
    <div className={`${classes['banner']} ${classes['alignment']}`}>
    </div>
  )
}
